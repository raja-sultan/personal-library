"use client";

import { CustomAccessComponentSection } from "@sections/settings/permissions/custom-access-component";

function CustomAccess(props): JSX.Element {
  return <CustomAccessComponentSection {...props} />;
}

export default CustomAccess;
