"use client";

import { PreviewExperienceSection } from "@sections/settings/preview-experience";

function PreviewExperience(): JSX.Element {
  return <PreviewExperienceSection />;
}

export default PreviewExperience;
