
export const style = {
    formButton: (theme) => ({
    height:"40px",
      color: theme.palette.mode === "dark" ? "neutral.300" : "neutral.700",
      borderColor: (theme.palette.neutral[300]),
    }),
  };