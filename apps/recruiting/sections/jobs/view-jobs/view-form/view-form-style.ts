// import { Theme } from "@mui/material/styles";

export const style = {
  formDiv: (theme) => ({
    p: 2,
    // maxHeight: "250px",
    // overflow: "auto",
    border: `1px solid ${theme.palette.neutral[300]}`,
  }),
  formTitle: () => ({
    marginBottom: "16px",
  }),
};
