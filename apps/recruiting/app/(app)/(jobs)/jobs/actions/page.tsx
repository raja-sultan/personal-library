"use client";
import { ViewActionMain } from "@sections/jobs";

function ViewJobsActions({ searchParams }): JSX.Element {
  return <ViewActionMain params={searchParams} />;
}
export default ViewJobsActions;
