"use client";

import { ConfigurePoolSection } from "@sections/crm/configure-pool-section";

function ConfigurePool(): JSX.Element {
  return <ConfigurePoolSection />;
}
export default ConfigurePool;
