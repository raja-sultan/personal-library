import { Typography } from "@mui/material";

export function ConfirmationEmail(): React.JSX.Element {
  return (
    <Typography variant="subtitle2" fontWeight={400} color='neutral.500'>
      Send a confirmation email immediately after scheduling 1-on-1
    </Typography>
  );
}
