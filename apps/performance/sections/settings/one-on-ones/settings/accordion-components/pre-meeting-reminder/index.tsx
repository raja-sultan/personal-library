import React from "react";
import { Typography } from "@mui/material";

export function PreMeetingReminder(): React.JSX.Element {
  return <Typography variant="subtitle2" fontWeight={400} color='neutral.500'>Send a Pre meeting reminder before starting 1-on-1 </Typography>;
}
