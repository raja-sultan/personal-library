import React from "react";
import { CreateAttribute } from "@sections/settings/user-atributes/create-attribute";

function AddAttribute(): JSX.Element {
  return <CreateAttribute />;
}

export default AddAttribute;
