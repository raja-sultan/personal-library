export const styles = {
  cardWrapper: (theme) => ({
    borderRadius: "10px",
    boxShadow: theme.shadows[16],
    outline: "none",
    border: "none",
    backgroundColor: "background.paper",
  }),
  avatar: {
    width: "32px",
    height: "32px",
  },
};
